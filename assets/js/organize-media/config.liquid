---
permalink: /assets/js/organize-media/config.js
---

export const CLIENT_ID = '{{ site.upload_config.google_client_id }}';
export const MAX_FILE_SIZE_BYTES = {{ site.upload_config.max_file_size_bytes }};
export const SIGNED_URL_ENDPOINT = '{{ site.upload_config.signed_url_endpoint }}';
export const MOCK_UPLOAD = {{ site.upload_config.mock_upload }};

export const ALLOWED_CONTENT_TYPES = {
{%- for item in site.upload_config.allowed_content_types %}
  '{{ item.ext }}': '{{ item.type }}'{% unless forloop.last %},{% endunless %}
{%- endfor %}
};
